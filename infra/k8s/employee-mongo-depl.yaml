apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-mongo
  template:
    metadata:
      labels:
        app: employee-mongo
    spec:
      containers:
        - name: employee-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: employee-mongo-srv
spec:
  selector:
    app: employee-mongo
  type: ClusterIP
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017